# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
target/
*.tsbuildinfo
.next/

# Native binaries (built per-platform via CI)
*.node

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
.idea/

# Tests / coverage
coverage/

# pnpm / yarn
.pnpm-debug.log

# Cargo
Cargo.lock.bak

# Claude / tooling
.claude/projects/
.claude/settings.local.json

# === Migration-time exclusions ===
# 既存の sibling directory が新 repo に取り込まれないようにする。
# pleasync-client は subtree で packages/client/ に統合済み。
# pleasync-test は examples/nextjs/ に移動済み。
# pleasync-desktop は今は別 repo のまま運用継続。
pleasync-client/
pleasync-desktop/
pleasync-client.bak/

# Next.js auto-generated
next-env.d.ts
